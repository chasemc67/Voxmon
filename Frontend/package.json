{
  "name": "nft-minter",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@alch/alchemy-web3": "^1.1.1",
    "@metamask/detect-provider": "^1.2.0",
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "axios": "^0.21.1",
    "classnames": "^2.3.1",
    "firebase": "^9.1.3",
    "node-sass": "^4.14.1",
    "react": "^16.13.1",
    "react-countdown": "^2.3.2",
    "react-dom": "^16.13.1",
    "react-router-dom": "^5.2.0",
    "react-scripts": "4.0.3",
    "use-wallet": "^0.10.0",
    "web-vitals": "^1.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "functions": "cd functions/voxmon && npm install && cd ../saveEmail/ && npm install && cd ../preview && npm install && cd ../submitVote && npm install && cd ../token && npm install"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "dotenv": "^8.6.0"
  }
}
